# Clearance Deals Backend (Test Route)

This version includes a test /api/deals route that returns dummy data.

## TO PUSH:

1. Unzip the folder
2. Git Bash Here
3. Run:

```
git add .
git commit -m "add test route"
git push
```

Render will auto-deploy.
